# BACKLOG

Ideas deferred out of the MVP, and transcript-only ideas recorded for
provenance (BUILD_BRIEF §1.3: the transcript is not an implementation spec —
material transcript-only ideas live here rather than being built).

## Explicitly out of MVP scope (BUILD_BRIEF §4.2)

Autonomous sending; SMS; Outlook; Slack; CRM integrations; billing; mobile
apps; fine-tuning; full enterprise search; arbitrary agent actions; multi-user
administration; public self-service signup; complex enterprise SSO; automatic
activation of learned rules; automatic attachment processing; automatic Git
commits containing user correspondence or company knowledge.

## Future hosted-pilot work

- Cloud Pub/Sub push-notification trigger mode (BUILD_BRIEF §8.3) with webhook
  validation.
- Multi-user authorization design and hosted PostgreSQL deployment (Phase 12).

## Future backlog items (recorded 2026-06-12)

- **SMS support** — explicitly out of MVP scope (BUILD_BRIEF §4.2); remains a
  future backlog item.
- **n8n / Lovable workflow mappings** — integrations or mappings to n8n and
  Lovable remain future backlog items; not part of the MVP.

## Constraints recorded for later phases (2026-06-12)

- **Historical sent-mail analysis** (Phases 2/8): explicitly user-selected,
  marked baseline, and creates **pending proposals only**. It never silently
  activates rules and never drafts historical messages. See
  `docs/LEARNING_MODEL.md`.
- **Phase 3 triage evaluation** must report labeled counts, false positives,
  false negatives, and uncertain cases. See `docs/EVALUATION.md`.

## Transcript-only ideas

_None recorded yet._ When an idea appears only in
`docs/source-material/` and is material but not part of the approved brief,
capture it here with a short note and date rather than implementing it.
