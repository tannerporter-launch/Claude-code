# source-material

Provenance only. Per BUILD_BRIEF §1.3, the original conversation transcript is
**not** an implementation specification — it ranks lowest in the source-of-truth
hierarchy. Do not implement functionality solely because it appears here;
record material transcript-only ideas in `docs/BACKLOG.md`.

The canonical layout reserves `original-conversation-transcript.docx` in this
directory. It is not committed here (it may contain correspondence and is not
required to build the product). Add it locally if needed for reference; it must
never become a build dependency.
